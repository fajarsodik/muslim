import { useStore } from "../stores/store";

const PrayerTimeMonthly = () => {
  const { prayerTimes } = useStore();
  return (
    <div>
      <p>test</p>
    </div>
  );
};

export default PrayerTimeMonthly;
